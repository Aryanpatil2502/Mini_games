[project]
name = "mini-games"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sam7895", email = "sameerpatil2502@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
]

[project.scripts]
mini-games = "mini_games:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
